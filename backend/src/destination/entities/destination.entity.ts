// Placeholder from the CLI's `nest g resource` scaffold. This resource
// currently uses raw SQL (see interfaces/destination.interfaces.ts for
// the query result shape), not an ORM, so this entity isn't wired up to
// anything yet. Kept in case we adopt TypeORM later.
export class Destination {}
