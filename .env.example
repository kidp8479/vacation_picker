POSTGRES_USER=changeme
POSTGRES_PASSWORD=changeme
POSTGRES_DB=vacation_picker
NODE_ENV=development
PORT=3000

# Host-side ports for docker compose, change these if they conflict
# with something already running on your machine
POSTGRES_PORT=5432
BACKEND_PORT=3000
FRONTEND_PORT=5173
